----


-- add gold to item brief (colored in yellow)

-- cmds
        -- map msdp (enable on or off)
        -- map locate (enable msdp, find, disable msdp)
        -- make all maps read-only



-- getting summoned should reorient map
        Neytiri has summoned you!



-- auto-cast should just keep firing until all met
        -- double fires now


-- manually fix/verify all paths
        -- hao -> uw does not work

-- handle Star Wars flight
-- handle Charon flight in UW


-- create room terrain cmd
        room terrain s FOREST
                -- dig south
                -- make a void room in between
                -- make void room == current room terrain
                -- make new room FOREST
                -- set directions to {14} instead of {4} for south, 


                room terrain <DIR> <TERRAIN>
                #map dig s -- $DIR
                #map insert s -- $DIR
                #map mov s -- $DIR
                #map mov s -- $DIR
                #map set roomsymbol {<F0B5345>⍋ } -- switch on $TERRAIN
                #map set roomterrain FOREST -- $TERRAIN
                #map mov n -- switch on $DIR
                #map set roomsymbol {<F21618C>~ } -- switch on $MSDP_ROOM[TERRAIN]
                #map set roomterrain $MSDP_ROOM[TERRAIN]
                #map mov n -- switch on $DIR


{AREA}{The Bardic Colleges}{DOORS}{}{EXITS}{{northeast}{4700}}{NAME}{Bardic College Supply
Shop}{TERRAIN}{INSIDE}{VNUM}{4703}

-- add regenerate to auto cast spells
-- update legend for shops, and other landmarks (specifically color?)
-- quick cmd for getting map stats, 
        -- jt map stats brief -- just the summary
        -- and jt map stats all -- list all in order
-- map list with current state
-- map-stats do not look for distinct areas when calculating linked areas, it counts all links
-- finish linking
        Hell (inside Arachnos)
        Golden Pagoda	
        Goth Camps	
        Metro	
        Scipio Dreaming	
        Shadow Rift	
        Shadow Storm	
        (Skara Brae)	




    #map TERRAIN FOREST <F0B5345>⍋ WIDE FADEOUT;
    #map TERRAIN INSIDE <F922B21>≡;
    #map TERRAIN CITY <FF03A2E>= WIDE;
    #map TERRAIN FIELD <F9A7D0A>· WIDE FADEOUT;
    #map TERRAIN NOSWIM <F21618C>~;
    #map TERRAIN SWIM <F21618C>~;
    #map TERRAIN MOUNTAINS <F935116>◮ WIDE;
    #map TERRAIN HILLS <F7D6608>︵  DOUBLE WIDE DENSE FADEOUT;
    #map TERRAIN MIDAIR <F6C3483>⊰ SPARSE WIDE;

